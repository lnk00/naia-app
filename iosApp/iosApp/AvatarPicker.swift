import SwiftUI

struct AvatarPicker: View {
    var body: some View {
        VStack {
            Text("A quoi ressemble Damien ?").font(.largeTitle.weight(.black)).multilineTextAlignment(.center)
            Spacer()
        }
    }
}